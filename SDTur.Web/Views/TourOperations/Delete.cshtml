@model SDTur.Web.Models.Tour.Core.TourOperationViewModel
@{
    ViewData["Title"] = "Tur Operasyonu Sil";
}

<div class="row mb-4">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center">
            <h1 class="h3 mb-0">
                <i class="bi bi-trash text-danger me-2"></i>Tur Operasyonu Sil
            </h1>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left me-2"></i>Geri Dön
            </a>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white">
                <h5 class="card-title mb-0">
                    <i class="bi bi-exclamation-triangle text-warning me-2"></i>Silme Onayı
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <h6 class="alert-heading">
                        <i class="bi bi-exclamation-triangle me-2"></i>Dikkat!
                    </h6>
                    <p class="mb-0">Bu operasyon kaydını silmek istediğinizden emin misiniz? Bu işlem geri alınamaz.</p>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-airplane me-1"></i>Tur
                        </label>
                        <div class="d-flex align-items-center">
                            <div class="bg-primary bg-opacity-10 rounded-circle p-2 me-2">
                                <i class="bi bi-airplane text-primary"></i>
                            </div>
                            <div>
                                <div class="fw-semibold">@Model.TourName</div>
                                <small class="text-muted">@Model.Description</small>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-gear me-1"></i>Operasyon Türü
                        </label>
                        <div>
                            <span class="badge bg-info bg-opacity-75 fs-6">@Model.OperationType</span>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-person me-1"></i>Çalışan
                        </label>
                        <div class="d-flex align-items-center">
                            <div class="bg-success bg-opacity-10 rounded-circle p-2 me-2">
                                <i class="bi bi-person text-success"></i>
                            </div>
                            <span class="fw-semibold">@Model.EmployeeName</span>
                        </div>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-calendar me-1"></i>Operasyon Tarihi
                        </label>
                        <div class="h6 mb-0">@Model.OperationDate.ToString("dd MMMM yyyy HH:mm")</div>
                        <small class="text-muted">@Model.OperationDate.ToString("dddd")</small>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-currency-dollar me-1"></i>Maliyet
                        </label>
                        <div class="h4 fw-bold text-warning mb-0">
                            @Model.Cost.ToString("C")
                        </div>
                    </div>
                    
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-flag me-1"></i>Durum
                        </label>
                        <div>
                            @if (Model.IsCompleted)
                            {
                                <span class="badge bg-success fs-6">
                                    <i class="bi bi-check-circle me-1"></i>Tamamlandı
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-warning fs-6">
                                    <i class="bi bi-clock me-1"></i>Devam Ediyor
                                </span>
                            }
                        </div>
                    </div>
                </div>

                @if (!string.IsNullOrEmpty(Model.Notes))
                {
                    <div class="mb-3">
                        <label class="form-label fw-semibold text-muted">
                            <i class="bi bi-text-paragraph me-1"></i>Notlar
                        </label>
                        <div class="p-3 bg-light rounded">
                            @Model.Notes
                        </div>
                    </div>
                }

                <hr class="my-4">
                
                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />
                    
                    <div class="form-check mb-3">
                        <input class="form-check-input" type="checkbox" id="confirmDelete" required>
                        <label class="form-check-label" for="confirmDelete">
                            Operasyon kaydının silineceğini ve bu işlemin geri alınamayacağını anlıyorum
                        </label>
                    </div>
                    
                    <div class="form-check mb-3">
                        <input class="form-check-input" type="checkbox" id="confirmDataLoss" required>
                        <label class="form-check-label" for="confirmDataLoss">
                            Operasyon kaydına ait tüm verilerin kalıcı olarak silineceğini kabul ediyorum
                        </label>
                    </div>
                    
                    <div class="d-flex justify-content-end gap-2">
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-x-circle me-2"></i>İptal
                        </a>
                        <button type="submit" class="btn btn-danger" id="deleteBtn" disabled>
                            <i class="bi bi-trash me-2"></i>Operasyon Kaydını Sil
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-lg-4">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white">
                <h5 class="card-title mb-0">
                    <i class="bi bi-info-circle me-2"></i>Bilgi
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-danger">
                    <h6 class="alert-heading">
                        <i class="bi bi-exclamation-triangle me-2"></i>Önemli Uyarı
                    </h6>
                    <ul class="mb-0 small">
                        <li>Bu işlem geri alınamaz</li>
                        <li>Operasyon kaydı kalıcı olarak silinecektir</li>
                        <li>İlgili raporlar etkilenebilir</li>
                        <li>Silme işlemi öncesi yedek almanız önerilir</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
