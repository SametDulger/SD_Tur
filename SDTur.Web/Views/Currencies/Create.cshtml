@model SDTur.Web.Models.Master.References.CurrencyCreateViewModel
@{
    ViewData["Title"] = "Yeni Para Birimi";
}

<div class="container">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">
                    <i class="bi bi-plus-circle text-primary me-2"></i>Yeni Para Birimi
                </h1>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left me-2"></i>Geri Dön
                </a>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-header border-0">
                    <h5 class="card-title mb-0">
                        <i class="bi bi-form me-2"></i>Para Birimi Bilgileri
                    </h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger shadow-sm" role="alert"></div>
                        
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="Code" class="form-label">
                                    <i class="bi bi-hash me-1"></i>Kod <span class="text-danger">*</span>
                                </label>
                                <input asp-for="Code" class="form-control" placeholder="USD, EUR, TRY..." required />
                                <span asp-validation-for="Code" class="text-danger small"></span>
                            </div>
                            
                            <div class="col-md-6">
                                <label asp-for="Symbol" class="form-label">
                                    <i class="bi bi-symbol me-1"></i>Sembol <span class="text-danger">*</span>
                                </label>
                                <input asp-for="Symbol" class="form-control" placeholder="$, €, ₺..." required />
                                <span asp-validation-for="Symbol" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="Name" class="form-label">
                                    <i class="bi bi-currency-exchange me-1"></i>Para Birimi Adı <span class="text-danger">*</span>
                                </label>
                                <input asp-for="Name" class="form-control" placeholder="Amerikan Doları, Euro, Türk Lirası..." required />
                                <span asp-validation-for="Name" class="text-danger small"></span>
                            </div>
                            
                            <div class="col-md-6">
                                <label asp-for="ExchangeRate" class="form-label">
                                    <i class="bi bi-graph-up me-1"></i>Döviz Kuru <span class="text-danger">*</span>
                                </label>
                                <input asp-for="ExchangeRate" type="number" step="0.0001" class="form-control" placeholder="1.0000" required />
                                <span asp-validation-for="ExchangeRate" class="text-danger small"></span>
                            </div>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <div class="form-check form-switch">
                                    <input asp-for="IsActive" class="form-check-input" type="checkbox">
                                    <label class="form-check-label">
                                        <i class="bi bi-toggle-on me-1"></i>Aktif
                                    </label>
                                </div>
                                <small class="text-muted">Bu para biriminin aktif olduğunu işaretleyin</small>
                            </div>
                        </div>

                        <hr class="my-4">

                        <div class="d-flex justify-content-end gap-2">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-x-circle me-2"></i>İptal
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle me-2"></i>Kaydet
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm">
                <div class="card-header border-0">
                    <h5 class="card-title mb-0">
                        <i class="bi bi-info-circle me-2"></i>Bilgi
                    </h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-info shadow-sm">
                        <h6 class="alert-heading">
                            <i class="bi bi-lightbulb me-2"></i>Para Birimi Oluşturma
                        </h6>
                        <ul class="mb-0 small">
                            <li>Para birimi kodu ve sembolünü girin</li>
                            <li>Para birimi adını belirleyin</li>
                            <li>TL karşılığı döviz kurunu ayarlayın</li>
                            <li>Aktiflik durumunu işaretleyin</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
