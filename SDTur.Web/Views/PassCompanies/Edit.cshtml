@model SDTur.Web.Models.Master.Pass.PassCompanyEditViewModel
@{
    ViewData["Title"] = "Pas Şirketi Düzenle";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">
                    <i class="bi bi-list-ul text-primary me-2"></i>Pas Şirketi Düzenle
                </h1>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left me-2"></i>Geri Dön
                </a>
            </div>
        </div>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <p class="text-muted">Bu sayfa henüz tamamlanmamıştır.</p>
        </div>
    </div>
</div>
